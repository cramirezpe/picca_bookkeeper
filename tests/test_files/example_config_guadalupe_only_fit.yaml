general:
  conda environment: picca
  system: slurm_cori
  defaults file: dMdB20
  
data:
  bookkeeper dir: results_directory/fit_var
  
  healpix data: /global/cfs/cdirs/desi/spectro/redux/guadalupe/healpix/
  catalog: QSO_CATALOG
   
delta extraction:
  use existing: results_directory
   
correlations:
  use existing: results_directory

fits:
  auto correlations: lya.lya-lya.lya
  cross correlations: lya.lya 


  metals: False

  remove default args:
    vega_main:
      general:
        sample:
          ap:
          at: